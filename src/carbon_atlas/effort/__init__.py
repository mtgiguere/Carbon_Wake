"""Trawling-effort domain model: the 0.01-degree grid and effort aggregation.

Pure — stdlib only, same discipline as :mod:`carbon_atlas.reactivity` (see
docs/ARCHITECTURE.md §3). File formats and downloads live in the ingest layer
above; this package only models grid cells and effort quantities.
"""
