"""Seafloor carbon domain model: quantities that never travel without their
uncertainty.

Pure — stdlib only, same discipline as :mod:`carbon_atlas.reactivity` (see
docs/ARCHITECTURE.md §3). Raster reading lives in the ingest layer above; this
package only models the quantities the rasters describe.
"""
